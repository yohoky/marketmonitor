<div align="center">

![Marketmonitor](assets/icon_256.png)

# Marketmonitor

**Windows 桌面常驻的 A 股 / 港股 悬浮小组件** —— **多个监控列表各自一个悬浮窗**（股票 / 基金 / 指数分开盯）、实时行情、异动提醒（系统通知 / 提示音 / **邮件推送** / **邮件定时汇总**），收件邮箱 1~5 个可分别指定，配置一键导出导入，客户端自动更新，背景 / 文字透明度分别可调、黑白摸鱼模式，右键即配置，装完即用。

**v1.5.0 · MIT License · 无需账号 · 免费无 Key**

</div>

---

## ✨ 它是什么

一个常驻在 Windows 桌面（默认右下角）的透明小卡片，实时显示你自选股票的**名称 / 现价 / 涨跌幅**：

- **多个监控列表 = 多个独立小窗**：列表 1 盯自选股、列表 2 盯板块指数、列表 3 盯基金……各自位置、尺寸、透明度、提醒阈值、收件人都可以不一样
- 多只股票同屏，可**平滑滚动**或**逐页跳动**两种展示方式自由切换
- 涨用**红**、跌用**绿**（遵循 A 股惯例）
- 后台每 30 秒静默刷新一次实时行情，全程不打断你
- 完全透明可叠加在任意窗口之上，或调成半透明"摸鱼"不刺眼

## 🖼 预览

**实时显示** —— 常驻桌面的小卡片，实时行情 + 休市状态：

![实时显示界面](assets/widget.png)

**设置界面** —— 右键卡片即可打开，列表 / 标的 / 外观 / 位置 / 异动提醒 / 邮件一键配置：

![设置界面](assets/settings.png)

## 🚀 安装（最简单）

1. 到 [Releases](../../releases) 页面，下载最新的 `Marketmonitor-Setup-x.x.x.exe`（一键安装包，约 70MB）。
2. 双击 → 自动安装到当前用户目录（**无需管理员权限**）→ 自动生成**桌面 + 开始菜单**快捷方式 → 装完自动启动。
3. 想卸载：设置 → 应用 → 搜 "Marketmonitor" → 卸载即可（会一并清理配置）。

> 便携版不安装、免依赖，也可以直接把发布包里的程序文件夹拷到任意 U 盘即插即用。

> 🔄 **升级不用手动下载**：程序启动 20 秒后会静默检查一次新版本（之后每 24 小时一次），有新版会弹通知；也可以在「设置 → 关于与更新」点「检查更新」→「下载并安装新版」，程序会下载安装包并拉起安装向导。

## ⚙️ 功能一览

| 功能 | 说明 |
|------|------|
| 🗂 多监控列表 | 每个列表 = **一个独立的悬浮小窗**，最多 6 个。可分别盯自选股 / 板块指数 / 基金 / 港股……各自拥有标的、窗口尺寸与位置、透明度、黑白模式、切换方式、异动阈值、收件邮箱、定时汇总；设置页顶部 Tab 切换，托盘「监控列表」子菜单可单独显隐 |
| 📈 自选管理 | 支持 **A 股 / 基金(ETF) / 可转债 / 港股**：输入 `600519`、`510300`(基金)、`128050`(可转债)、`00981`(港股 5 位) 或 `hk00981` / `sh600519` 自动识别市场；支持拼音缩写搜索（`zxgj` 可带出港股候选）；支持批量粘贴（逗号/空格/换行） |
| ⤒ 一键置顶 / 置底 | 监控列表里点 `⇧` 直接把某只调到最前、点 `⇩` 调到末尾，剩下的用 `↑ / ↓` 微调（小组件按此顺序展示）|
| 📊 板块指数 | 内置 **11 个宽基指数**（上证指数 / 深证成指 / 创业板指 / 沪深300 / 中证500 / 中证1000 / 上证50 / 国证2000 / 中小100 / 科创50 / 恒生指数）+ **32 个板块指数**（食品饮料 / 白酒 / 医药 / 医疗 / 生物医药 / 银行 / 证券 / 保险 / 电子 / 芯片 / 算力 / 传媒 / 军工 / 新能车 / 环保 / 煤炭 / 油气 / 有色 / 化工 / 地产 / 农业…），勾选即加入**当前列表**，同屏展示并参与滚动 / 跳动与异动、邮件提醒 |
| 🔤 名称自动补全 | 只有代码没有名称时，自动从行情 / 接口解析成中文名并落库（列表优先展示名称）|
| 🔔 异动提醒 | **按列表独立配置**：涨幅/跌幅**分别设置**阈值（默认各 3.9%），**默认仅在开盘时段提醒**，可选「提醒市场」= A股 / 港股 / 两者（A股 09:25–11:35 / 12:55–15:05；港股 09:00–12:00 / 13:00–16:00，周末除外，也可整体关闭）；触发时系统通知 + 提示音 + **对应列表的**卡片闪烁，冷却去重不刷屏 |
| 📮 收件邮箱槽位 | 全局设 1~5 个收件邮箱（每个都能单独启用 / 停用），再由**每个列表挑选自己发给哪几个**；发件账号（SMTP 账号 + 授权码）全局共用一份 |
| 📧 邮件推送 | 触发异动后把明细发到该列表指定的收件人；阈值**默认与异动提醒一致**，也可单独设置（例如异动 3.9% 就提醒、邮件只在 6% 才发）；支持 465(SSL) 与 587(STARTTLS)，账号授权码只存在本机 `config.ini`，不上传任何服务器 |
| 🗂 邮件定时汇总 | **按列表独立开关**：每隔 N 分钟（1–1440，默认 30）把该列表的行情汇总成一封邮件发出。排版是**手机友好的 HTML 卡片**（竖排、不用横滑、涨红跌绿 + 涨跌家数概览条），同时附带纯文本版兜底；与异动推送**两条通道互不影响**（异动是"到阈值才发"，汇总是"到点就发"），另提供「立即给当前列表发一封汇总」手动验证 |
| 💾 配置导出 / 导入 | 一键把**全部列表 + 邮件设置**导出成一个 JSON，换台电脑装上程序后导入即可复现，不用重新配一遍；导入前会自动把原配置备份成 `config.ini.bak`。可选是否包含邮箱授权码 |
| 🔄 客户端自动更新 | 用 GitHub Releases 当更新服务器（零额外依赖）：启动 20 秒后静默检查、之后每 24 小时一次，有新版弹系统通知；设置页可手动「检查更新 → 下载并安装新版」（下载后拉起官方安装向导，不会静默覆盖）|
| 🎚 背景透明度 | 0.05–1.0 可调，**只淡卡片底色、文字不受影响**（低透明度下自动收敛光晕，不发虚）|
| 🔅 文字透明度 | 0.05–1.0 **独立可调**，只淡文字和数字（含状态栏），卡片轮廓保持原样；与背景透明度互不干扰，可组合出"只剩淡淡一层数字"的深度摸鱼效果 |
| 🌗 黑白模式 | 打开后涨跌**不再用红绿**，改深浅灰（涨深灰 / 跌浅灰），异动闪烁也一并转为中性灰，屏幕前不显眼；一行开关，立即生效 |
| 📐 尺寸 | 宽 / 高**双向实时**生效，高度决定同屏行数（高 140≈3 行，高 400≈12+ 行）|
| 🧭 屏幕位置 | 九宫格一键归位（左上 / 正中 / 右下 ……），也可随意拖动；**默认右下角**。新列表会自动避开已有窗口找空位 |
| 🔄 展示方式 | `滚动` 平滑位移 / `跳动` 整页切换，实时切换 |
| ⏱ 动画节奏 | **滚动速率**与**跳动间隔**分别可调（滚动每行 0.8–15 秒、跳动每页 1–20 秒），两个模式各记各的，互不覆盖 |
| 📌 置顶 | 一键让小组件常在最上层（按列表分别设置）|
| 🖱 拖拽 | 按住卡片任意非按钮区域拖动到任意位置，松手自动保存，下次启动仍在原地 |
| ⏸ 异动冷却 | 提醒防抖（默认 3 分钟），避免同一异动反复轰炸；本机提醒与邮件各自独立计时，不同列表之间也互不干扰 |

## 🖲 常用操作

| 操作 | 效果 |
|------|------|
| 按住卡片拖动 | 移动位置（自动记忆）|
| **右键卡片** | 打开设置页，并自动切到该卡片对应的列表 Tab |
| 托盘图标 **左键** | 显示 / 隐藏全部卡片 |
| 托盘图标 **右键** | 显示/隐藏全部 · **监控列表**（每个列表：打开设置 / 显示 / 隐藏）· 设置 · 立即刷新 · **检查更新** · **关于（版本号 + 发布日期）** · 退出 |

> 设置窗口顶部还有 **帮助 → 关于 Marketmonitor**，可随时查看版本号与发布日期。

> 所有设置即时写入 `config.ini` 并持久化，重启后保留。从 1.4.x 升级上来的旧配置会自动迁移成多列表结构，原文件备份为 `config.ini.legacy`。

## 🧩 技术栈

- **Electron 31** + 原生 HTML / CSS / JS（**零前端框架**，包体小、无依赖地狱）
- 数据源：腾讯财经行情接口 `qt.gtimg.cn`（免费、无需注册、无需 Key）
- 邮件：**自写的零依赖 SMTP 客户端**（EHLO / STARTTLS / AUTH LOGIN / DATA），不引入 nodemailer，保证热更新链路只替换 js 文件
- 存储：纯 `config.ini`（INI 文本，`[general]` + `[email]` + 每个列表一个 `[list:xx]` 段），随程序目录走，天然便携
- 自动更新：GitHub Releases 公开 API（`releases/latest`）+ 系统安装器，不引入 electron-updater
- 打包：electron-builder（NSIS 一键安装包 / 便携包）

## 🏗 从源码构建

```bash
# 1. 安装依赖（会自动拉 Electron）
npm install

# 2. 本地运行调试
npm start

# 3. 打一键安装包（得到 dist/Marketmonitor-Setup-x.x.x.exe）
npm run dist
```

> 构建目标为 Windows x64。

> 🤖 **不用自己构建也行**：仓库已配置 GitHub Actions 自动构建（`.github/workflows/build.yml`），每次 push / 打 tag 都会在云端自动生成 Windows 安装包，产物可在 **Actions** 页下载。

## 📁 文件结构

```
marketmonitor/
├── .github/workflows/
│   └── build.yml           # CI：push / tag 自动构建 Windows 安装包并发布 Release
├── package.json            # 依赖 + electron-builder 打包配置 + 版本号（自动更新的比较基准）
├── package-lock.json       # 锁文件（可复现构建）
├── main.js                 # Electron 主进程（多列表窗口注册表 / 托盘 / 行情抓取 / 提醒引擎 / 邮件 / 配置 / 自动更新）
├── preload.js              # contextBridge IPC 桥（安全隔离）
├── renderer/
│   ├── index.html          # 小组件主界面（每个列表窗口共用一套）
│   ├── renderer.js         # 渲染层逻辑（滚动/跳动/亮度/拖拽）
│   ├── style.css           # 小组件样式（背景 --card-a / 文字 --text-a 分别控制 + 黑白模式）
│   ├── settings.html       # 设置页（列表 Tab / 个股 / 指数 / 提醒 / 邮件与收件槽位 / 汇总 / 外观 / 尺寸 / 位置 / 导出导入 / 关于与更新）
│   ├── settings.css
│   └── settings.js
└── assets/
    └── icon.png / icon.ico / icon_256.png
```

## 🗃 配置文件结构

`config.ini` 就放在程序目录里（安装版为 `%LOCALAPPDATA%\Programs\Marketmonitor`），可以直接用记事本改：

```ini
[general]
fetchIntervalMs=30000        ; 行情刷新间隔（所有列表共用一次请求）

[email]                      ; 全局发件账号 + 1~5 个收件槽位
enabled=true
host=smtp.qq.com
port=465
secure=true
user=you@example.com
pass=                        ; 邮箱授权码（不是登录密码）
box1=someone@example.com     ; 收件槽位 1
on1=true
box2=
on2=false                    ; 地址留着但不启用 = 临时停用

[list:l1]                    ; 列表 1：不含 "=" 的行 = 标的
name=自选股
width=220
height=84
mailboxes=1                  ; 这个列表发给槽位 1（写 1,3 就是发给槽位 1 和 3）
digestEnabled=true
digestIntervalMin=30
600519 贵州茅台
000001 平安银行

[list:l2]                    ; 列表 2：可以盯另外一批标的
name=板块指数
width=220
height=64
mailboxes=1
sh000001 上证指数
sz399001 深证成指
```

## ⚠️ 已知限制

- 已支持 **A 股 / 基金(ETF) / 可转债 / 港股 / 板块指数**；北交所（4 / 8 开头）仍需手动加 `sh/sz` 前缀
- 指数代码必须带前缀（`sh000001`）——只写 `000001` 会被识别成平安银行 `sz000001`
- 港股代码为 5 位（`00981`）；只写 3~4 位（`981`）时不会自动判市场，请写 `hk981` 或用拼音搜索选候选
- 港股只收录**正股**，窝轮 / 牛熊证等衍生品会在搜索里过滤掉
- 邮件推送需要你的邮箱**开启 SMTP 服务并生成授权码**（不是登录密码）：QQ / 163 等在邮箱「设置 → 账户」里开启。发送结果写在程序目录 `maillog.txt`，失败原因见 `error.log`
- 邮件定时汇总是"到点整点发"——程序每 30 秒刷新一次行情，所以实际发出时间会有最多 30 秒的偏差；关闭汇总开关时不会发出任何汇总邮件
- 每个列表的定时汇总**各自独立计时**；同一个列表的行情只发一封，不会因为多个窗口而重复发送
- 自动更新走 GitHub Releases：**需要能访问 github.com**（国内网络可能较慢）；下载后由系统安装向导完成覆盖，程序不会静默替换正在运行的 exe
- 监控列表上限 6 个（窗口太多既挤屏幕也容易误操作）
- 停牌股票显示最后一次有效数据
- 无网络时占位为 `--`，恢复后自动刷新
- 仅在 Windows x64 上验证过；macOS / Linux 未专门适配（透明无边框窗口行为可能有差异）

## ❤️ 写在最后

**这是 AI 从零搓出来的程序。**

从最初只有两行滚动的简陋卡片，到现在的异动提醒、透明度、尺寸双向调节、九宫格定位、一键安装包——每一条功能都是 AI 一步步落地、再反复修 bug 磨出来的。

其实我心里一直想换**体积更小**的技术方案（比如 Tauri），可这台电脑的硬盘实在太小，装不下那些开发支持文件和依赖，折腾几轮都跑不起来。最后就用 Electron 把这版本做扎实了——**这个版本，也不错。**

记录一下，分享给同样想"让 AI 帮我把一个小工具做出来"的朋友。

## 📄 License

[MIT](LICENSE) © 2026 (yohoky)
